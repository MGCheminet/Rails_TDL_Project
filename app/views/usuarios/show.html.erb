<div class="container">
  <div class="row">
    <div class="col-md-6 offset-md-3">
      <div class="card">
        <div class="card-body">
          <h2 class="card-title">Mis Datos</h2>
          <p><strong>Email:</strong> <%= @usuario.email %></p>
          <p><strong>Nombre:</strong> <%= @usuario.nombre %></p>

          <%= link_to 'Volver', usuarios_path, class: 'btn btn-primary' %>
        </div>
      </div>
    </div>
  </div>
</div>
